############################################
# WARNING:
# Managed by puppet - DO NOT modify locally.
############################################

127.0.0.1	localhost.localdomain	localhost
<%= scope['::ipaddress'] %> <%= scope['base::servername'] %>.<%= scope['base::domain'] %> <%= scope['base::servername'] %>

<% scope['hosts::entries'].each do |entry| -%>
<%= entry %>
<% end -%>
