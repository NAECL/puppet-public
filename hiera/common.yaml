---

wordpress::config::sites:
  site1:
    sitename: 'www.local.com'
    dbname:   'www_local_com'

base::domain:       'local'

ntp::servers:
  - '0.centos.pool.ntp.org'
  - '1.centos.pool.ntp.org'

# ntp::servers:
#   - '0.amazon.pool.ntp.org'
#   - '1.amazon.pool.ntp.org'

