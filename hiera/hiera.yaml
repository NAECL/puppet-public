---
version: 5

defaults:
  datadir: hieradata
  data_hash: yaml_data

:hierarchy:
    - name: "Customised Yaml"
      path: "custom.yaml"
    - name: "Common To Everything"
      path: "common.yaml"
