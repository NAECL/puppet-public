---
version: 5

defaults:
  datadir: hieradata
  data_hash: yaml_data

:hierarchy:
    - name: "Hostname Yaml"
      path: "hostname.yaml"
    - name: "Environment Yaml"
      path: "environment.yaml"
    - name: "Common To Everything"
      path: "common.yaml"
