---
version: 5

defaults:
  datadir: hieradata
  data_hash: yaml_data

:hierarchy:
    - name: "Hostname Specific"
      path: "hostname.yaml"
    - name: "Environment Specific"
      path: "environment.yaml"
    - name: "Region Specific"
      path: "region.yaml"
    - name: "Global"
      path: "global.yaml"
