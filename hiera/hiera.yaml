---
version: 5

defaults:
  datadir: hieradata
  data_hash: yaml_data

:hierarchy:
    - name: "Common To Everything"
      path: "common.yaml"
    - name: "Node Specific"
      path: "${::fqdn}.yaml"
